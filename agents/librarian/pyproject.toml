[project]
name = "agent-librarian"
version = "0.1.0"
dependencies = [
    "logiclab-core",
    "logiclab-kit",
    "agno",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
